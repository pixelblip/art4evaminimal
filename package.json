{
  "name": "paint-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Simple dither paint for Galaxy A40 / Android APK",
  "scripts": {
    "serve": "node scripts/serve-and-publish.cjs",
    "serve:static": "npx --yes serve www -l 5173",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/share": "^6.0.4",
    "capacitor-email-composer": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
